{
  "configurations": [
    {
      "name": "frontend-mocked",
      "description": "vite dev server with MSW serving canned data — standalone, no backend/db",
      "cwd": "frontend",
      "command": "pnpm dev:mocked",
      "url": "http://localhost:3000",
      "port": 3000
    }
  ]
}
